name: service_comment
description: Highly extensible comment service for the super app - supports comments and replies across all domains.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  flutter:
    sdk: flutter

  # Core packages
  core_base:
    path: ../../core/core_base
  core_network:
    path: ../../core/core_network
  core_logging:
    path: ../../core/core_logging

  # Services
  service_interaction:
    path: ../service_interaction

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
